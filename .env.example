# Path to your photo library (read-only mount in production)
PHOTO_DIR=./photos

# Where the SQLite DB and generated posters live (writable volume)
DB_PATH=./data/library.db
POSTER_DIR=./data/posters

# API server port
PORT=3000
